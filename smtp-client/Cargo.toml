[package]
name = "neomutt-smtp-client"
version = "0.1.0"
edition = "2024"
description = "Thin wrapper over lettre for sending email"

[dependencies]
neomutt-core = { path = "../core" }
lettre = { version = "0.11", features = ["native-tls"] }
