[package]
name = "neomutt-config"
version = "0.1.0"
edition = "2024"
description = "Configuration for neomutt-rs — accounts, keybindings, TOML loading"

[dependencies]
toml = "0.8"
serde = { version = "1", features = ["derive"] }
thiserror = "2"
